Started with no data but collected many PDF files using Python Scripts. 
Then converted the contents of these PDF files into a text file, 
adjusted the format of these files as well as their contents, 
and then limited the contents of these text files using logic embedded in code. 
